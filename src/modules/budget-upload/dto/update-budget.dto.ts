import { PartialType } from '@nestjs/mapped-types';
import {
  IsDateString,
  IsDecimal,
  IsNotEmpty,
  IsNumber,
  IsString,
  IsOptional,
} from 'class-validator';

export class UpdateBudgetDto {}
